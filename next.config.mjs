/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com', 'www.vecteezy.com', 'scontent.fmnl4-1.fna.fbcdn.net'],
    },
};

export default nextConfig;
